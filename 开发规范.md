# Foundry Auras 开发手册 (Project Bible)

本文档是该项目的核心开发规范。**任何加入开发的 AI 代理都必须首先阅读此文件，并严格遵守其中的架构设计和编码标准。**

## 1. 项目愿景 (Project Vision)
我们的目标是在 Foundry VTT (FVTT) 中复刻《魔兽世界》插件 **WeakAuras (WA)** 的核心体验，并深度结合 FVTT 的特性。
*   **目标用户**: 需要自定义战斗视觉提示的高阶用户/GM。
*   **核心关键词**: `可视化编辑器 (IDE)`, `事件驱动`, `低代码`, `暗黑开发者风格`, `FVTT原生集成`。
*   **兼容性目标的**: Foundry VTT V13 (最低) 和 V14 (验证目标)。
*   **体验目标**: 提供丰富的预设 (Presets)，让不懂代码的用户也能通过点击-选择快速创建如低血量报警、轮到我了等实用光环。

## 2. 架构设计 (Architecture)

### 2.1 文件结构
```
FoundryAuras/
 module.json         # 核心清单 (定义入口 main.js 和 语言包)
 main.js             # 入口文件 (Hook 注册, 全局 Engine 初始化)
 styles.css          # 全局样式 (Dark Theme, WA 风格定义)
 languages/
    zh-cn.json      # [核心] 中文语言包 (Source of Truth)
    en.json         # [镜像] 英文语言包 (物理复制 content from zh-cn.json 以强制中文)
 scripts/
    manager.js      # [UI Controller] 核心配置界面逻辑 (SPA 架构)
    presets.js      # [New] 预设模版定义 (FVTT 特定场景的开箱即用光环)
    editor.js       # [Deprecated] 旧的独立编辑器，功能已合并入 manager.js
 templates/
     manager.hbs     # [UI View] 主配置界面模板 (Handlebars)
```

### 2.2 数据流 (Data Flow)
*   **存储**: 数据存储在 `game.settings.get("FoundryAuras", "auras")` 中。
*   **管理 (Write)**: `AuraManager` (manager.js) 读取 -> 用户修改 (Hot update via `submitOnChange`) -> 写入 Settings。
*   **执行 (Read)**: `AuraEngine` (main.js) 监听 Hooks (`updateActor` 等) -> 读取 Settings -> 匹配条件 -> 操作 DOM (`#foundry-auras-hud`)。
*   **Tour系统**: 使用 Foundry VTT 原生 Tour API，提供新手引导。Tour 数据存储在 `tours/guide.json`，本地化键使用 `FOUNDRYAURAS.Tour.*` 模式。

### 2.3 Tour 系统架构
*   **注册时机**: 在 `Hooks.on('ready')` 中注册，避免与 JSON 方法冲突。
*   **Tour ID**: `"FoundryAuras.guide"`，可恢复 (`canBeResumed: true`)。
*   **步骤结构**: 6个步骤，覆盖欢迎、侧边栏、预设、编辑器、扫描、保存。
*   **选择器**: 使用 CSS 类选择器 (`.wa-container`, `.wa-sidebar`, `button[data-action='preset']` 等)。

## 3. 编码规范 (Coding Standards)

### 3.0 沟通与交互语言
*   **规则**: AI 助手必须始终使用**中文**与用户进行交流。
*   **适用范围**: 所有的对话回复、解释说明以及后续加入开发规范的文字描述。

### 3.1  严禁使用 jQuery
*   **原因**: V13/V14 正在逐步废弃 jQuery，且原生 DOM API 性能更好。
*   **规则**:
    *   禁止使用 `$()`, `html.find()`, `parents()`, `children()`.
    *   **替代方案**: 使用 `querySelector()`, `querySelectorAll()`, `closest()`, `addEventListener()`.
    *   **例外**: 如果 Foundry 核心 API 返回的是 jQuery 对象，请立刻使用 `[0]` 解包为原生 Element。

### 3.2  严格本地化 (Localization First)
*   **禁止硬编码字符串**: 哪怕是 "Name" 或 "Type" 这种简单词。
*   **键名规范**: `FOUNDRYAURAS.[Context].[Key]` (例如 `FOUNDRYAURAS.Editor.Trigger`).
*   **语言文件策略**: 本项目目前**强制中文显示**。
    *   修改时，必须同时更新 `zh-cn.json` *和* `en.json` (使用完全相同的内容)。
    *   确保 `module.json` 注册了这两个语言路径。
*   **功能同步 (Feature Sync)**: 无论增加什么功能，只要涉及 UI 显示或文本输出，**必须**同步在 `languages/zh-cn.json` 中添加对应的条目，不得遗漏。

### 3.3  注释规范
*   关键逻辑段落必须包含 **中文注释**。
*   解释 "Why" 而不仅仅是 "What"。

### 3.4  前端美学 (WeakAuras Style)
*   **主题**: 极简暗黑 (`#1a1a1a`, `#212121`).
*   **高亮**: WA 金色 (`#ffcc00`) 用于选中状态、边框高亮。
*   **字体**: 使用等宽字体 (`Consolas`, `Monospace`) 营造 IDE/开发者工具的氛围。
*   **交互**: 
    *   点击侧边栏 -> 无刷新更新右侧内容 (Handlebars Re-render).
    *   输入框修改 -> 自动保存 (Submit On Change).
    *   直角设计 (No border-radius in UI elements)。

### 3.5 纯文本原则 (No Emojis)
*   **严禁使用 Emoji**: 禁止在代码、注释、UI文本、日志中使用任何 Emoji 表情 (如 ❤️, ✨, 👁️ 等)。
*   **图标规范**: 统一使用 FontAwesome (`<i class="fas fa-*"></i>`) 或 SVG 图像。

## 4. 参考资源 (References)

### 4.1 Foundry VTT API
*   **V13 API 文档**: https://foundryvtt.com/api/
*   **V14 API 文档**: https://foundryvtt.com/api/v14/index.html
    *   *注意*: 开发时优先查阅 V14 文档以确保前瞻兼容性。

### 4.2 WeakAuras (Design Reference)
*   **GitHub**: https://github.com/WeakAuras/WeakAuras2
*   **设计参考**: 参考其配置面板的布局、图标选择器交互、触发器逻辑分类。

### 4.3 D&D 5e System (Code Reference)
*   **GitHub**: https://github.com/foundryvtt/dnd5e
*   **注意**: 官方文档不全，需直接查阅源码来获取最新的 Actor/Item 数据结构和 Hooks 事件。

**注册Tour (在 ready hook 中):**
```javascript
if (game.tours) {
    const tourId = "FoundryAuras.guide";
    if (!game.tours.get(tourId)) {
        game.tours.register("FoundryAuras", "guide", {
            title: "FOUNDRYAURAS.Tour.Title",
            description: "FOUNDRYAURAS.Tour.Description", 
            canBeResumed: true,
            display: true,
            steps: [
                {
                    id: "welcome",
                    selector: ".wa-container",
                    title: "FOUNDRYAURAS.Tour.Step1.Title",
                    content: "FOUNDRYAURAS.Tour.Step1.Content"
                },
                // ... 更多步骤
            ]
        });
    }
}
```

**Tour JSON 结构 (tours/guide.json):**
```json
{
    "id": "FoundryAuras.guide",
    "title": "FOUNDRYAURAS.Tour.Title",
    "description": "FOUNDRYAURAS.Tour.Description",
    "canBeResumed": true,
    "display": true,
    "steps": [
        {
            "id": "step-id",
            "selector": ".css-selector",
            "title": "FOUNDRYAURAS.Tour.StepX.Title",
            "content": "FOUNDRYAURAS.Tour.StepX.Content"
        }
    ]
}
```

## 6. 项目待办 (Roadmap)
*   [x] 基础框架与本地化
*   [x] WA 风格 UI (Dark Theme + 2-Col Layout)
*   [x] **高优先级**: 预设系统 (Presets) - 提供常用 FVTT 场景 (HP, Turn, etc.)
*   [x] 实现光环条件的动态解析 (Condition Script Execution).
*   [x] **高优先级**: 动画系统增强 (更多 CSS 动画: 呼吸, 发光, 心跳).
*   [x] **高优先级**: 图标选择器优化 (集成 FVTT FilePicker).
*   [x] **高优先级**: 导入/导出优化 (批量导出、导出整个配置).
*   [x] 导入/导出字符串功能 (Base64). -- 已通过批量导出实现备份
*   [x] **功能增强**: 文本占位符扩展 (%hp_pct, %temp_hp, %ac, %move, %effect_time, %stacks).
*   [x] **文档维护**: 随时同步更新 README 文件.
*   [x] **联动功能**: JB2A / WebM 动画支持 (允许选择视频作为光环图标).
*   [x] **UI优化**: 删除操作二次确认，防止误触.
*   [x] **UI优化**: z-index层级优化，解决弹窗被界面挡住的问题.
*   [x] **功能增强**: 动画个性化选项 (闪烁速度、进度条方向等).
*   [x] **功能增强**: 视觉效果全面定制 (大小、颜色、边框、阴影、旋转、缩放).
*   [x] **功能增强**: 触发器系统扩展 (定时器触发、状态触发).
*   [x] **功能增强**: 条件编辑器改进 (简单条件构建器).
*   [x] **功能增强**: 音频支持 (音效触发).
*   [x] **数据兼容性**: 修复模块检测问题，确保旧数据能正确加载.
*   [x] **预设优化**: Bless监控预设，检测己方token的祝福术效果.
*   [x] **预览系统**: 实时预览更新，设置修改后即时反映.
*   [x] **预览系统**: 实时预览更新，设置修改后即时反映.
*   [x] 向用户展示如何以 agent 角色发起请求 （自动执行: 2026-02-14 14:43）
*   [ ] **高级功能**: 冷却时间追踪 (Cooldowns) - 适配 dnd5e.
*   [ ] **高级功能**: 视觉编辑器 (Visual Condition Builder) - 避免写代码.

### 最近已完成的自动化任务与同步记录

- [x] 增强自动运行器（支持 `run-and-update` / `run-all-and-update`，可回写 `开发规范.md`）
- [x] 自动运行器使用东八区时间戳（UTC+8）
- [x] Localization QA 报告已生成并保存为 `scripts/agents/localization_report.json`
- [x] 同步 `languages/zh-CN.json` -> `languages/en.json`（已创建备份 `languages/en.json.bak`）

变更摘要：

- 本次自动化执行时间（UTC+8）：2026-02-14 14:43 （已写回在相应待办项处）
- 主要文件：`scripts/agents/todo_runner.js`（增加写回支持与时区处理）、`scripts/agents/localization_report.json`（报告）、`languages/en.json.bak`（备份）、`languages/en.json`（同步后文件）
- 建议：若接受自动化修改，下一步可在 CI 中加入一个简单的脚本，定期运行本地化 QA 与全仓库 jQuery 检查，并在发现差异时自动创建 PR。

### 全仓库 jQuery 检查（自动化结果）

- 检查时间（UTC+8）：2026-02-14 22:46:30
- 报告文件：`scripts/agents/jquery_report.json`
- 匹配到的文件（包含 jQuery-like 模式）：
    - `main.js`  (模式: `.find(`)
    - `scripts/agents/todo_runner.js`  (模式: `$(`, `.find(`)
    - `scripts/api-docs.js`  (模式: `.find(`)
    - `scripts/editor.js`  (模式: `.find(`)
    - `scripts/manager.js`  (模式: `.find(`)

说明：已在 `scripts/agents/jquery_report.json` 中保存详细报告。下一步建议：

- 对 `main.js`、`scripts/api-docs.js`、`scripts/editor.js`、`scripts/manager.js` 进行逐一替换与审查（将 jQuery 调用替换为原生 DOM API 或解包 Foundry 返回的 jQuery 对象）。
- 如需，我可以立即为这些文件生成替换补丁并提交更改。

## 参考资源 (References)
*   **JB2A (Jules&Ben's Animated Assets)**: [GitHub Repository](https://github.com/Jules-Bens-Aa?tab=repositories) - 动画素材库参考.

*Created by User & Copilot (Gemini 3 Pro) - 2026-02-09*

## 已移除音频触发（原因与记录）

- **变更时间**：2026-02-10
- **变更概述**：应用户要求，移除 Manager 配置面板中的“音频触发”功能，并清理与之相关的模板、脚本和样式。
- **尝试与复现步骤（已执行的工作）：**
    - 将原始纯文本音频输入替换为 Foundry 原生的 `<file-picker type="audio">`，尝试利用 FVTT 的文件浏览器避免自定义 file input 的兼容问题。
    - 在 `styles.css` 中调整 `.wa-settings-scroll` 的 `padding-bottom`，并调整 `.wa-footer-actions` 的 `z-index`，为可滚动内容预留空间，避免被 footer 覆盖。
    - 在 `scripts/manager.js` 中实现并多次迭代 `_fixFilePickers()`：尝试在渲染后定位并修正 `file-picker` 的按钮位置（包括延迟重试 50ms、250ms），以应对 Foundry 在运行时对子节点的重排/重父操作。
    - 修复 `templates/manager.hbs` 中的模板嵌套/闭合标签问题，确保音频区域正确被包含在滚动容器内。
    - 同步并修正语言文件（`zh-cn.json` 与 `en.json`）中与音频相关的本地化键。

- **为什么未能稳定保留该功能（原因分析）：**
    1. Foundry 的 `file-picker` 在运行时会对内部 DOM 做动态操作（重渲染/重父），导致我们通过绝对定位或脚本把按钮固定在滚动流中时不断被重写。
    2. 尽管通过 CSS 与 JS 增加了延迟重试逻辑，但在不同环境（不同浏览器/Foundry 版本）下表现不一致，仍存在按钮被 footer 遮挡或脱离滚动流的风险。
    3. 为了避免引入不稳定的 hack（过多的 DOM 重排监听或高优先级 z-index 改动），在不破坏其他 UI 的前提下，最终决定移除该功能并保留后续可恢复的备份方案。

- **已完成的清理工作：**
    - 从 `templates/manager.hbs` 删除音频控制相关的 HTML 块。
    - 从 `scripts/manager.js` 删除/注释与音频预览、播放、清除相关的事件处理和辅助函数。
    - 从 `styles.css` 中移除 `.wa-audio-group` 等音频专用样式，并保留对滚动区与 footer 的合理间距调整。
    - 同步更新 `languages/zh-CN.json` 与 `languages/en.json` 中对应键值，避免残留未翻译文本。

- **建议与下一步：**
    1. 如需保留“可恢复”路径，我可以把删除的音频实现（模板片段、JS 函数、CSS 规则）归档到 `scripts/manager.audio-removed.js`（或 `backup/` 目录下的同名文件），便于日后重试或以更稳健的方式重构；请指示是否需要备份。
    2. 若要重新实现该功能，推荐方案：基于 Foundry V14 的插件 API 编写一个专属的小组件，而不是直接操作 `file-picker` 的内部 DOM；或者在 Foundry 提供稳定的钩子后再恢复该功能。

*Last Updated: 2026-02-10 (移除音频触发并记录原因)*
